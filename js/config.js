/* ============================================
   VICOMETAL — Configuration
   Centralised URLs and feature toggles
   ============================================ */

const VICOMETAL_CONFIG = Object.freeze({
    // HeavyForge Platform URLs
    // Change these when deploying to production
    PLATFORM_URL: "https://app.vicometal.pt",
    API_URL: "https://api.vicometal.pt",

    // For local development, uncomment below:
    // PLATFORM_URL: "http://localhost:3000",
    // API_URL: "http://localhost:3001",
});
